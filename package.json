{
  "name": "dat-protocol-buffers",
  "version": "1.0.3",
  "description": "A convenience wrapper for encoding and decoding common dat protocol buffers defined in the dat white paper",
  "main": "index.js",
  "scripts": {
    "test": "standard && tape test/*.js"
  },
  "author": "Benjamin Forster",
  "license": "ISC",
  "dependencies": {
    "brfs": "^1.4.3",
    "protocol-buffers": "^3.2.1"
  },
  "devDependencies": {
    "standard": "^10.0.2",
    "tape": "^4.6.3"
  },
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/e-e-e/dat-protocol-buffers"
  },
  "bugs": {
    "url": "https://github.com/e-e-e/dat-protocol-buffers/issues"
  }
}
